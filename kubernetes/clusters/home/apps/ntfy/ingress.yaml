apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ntfy
  namespace: ntfy
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`ntfy.syslabs.dev`)
      kind: Rule
      services:
        - name: ntfy
          port: 80
  tls: {}
