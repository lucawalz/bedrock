apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: litellm-root-redirect
  namespace: llm
spec:
  redirectRegex:
    regex: "^https?://[^/]+/?$"
    replacement: "https://llm.syslabs.dev/ui/"
    permanent: true
