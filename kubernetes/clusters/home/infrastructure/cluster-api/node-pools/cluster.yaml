apiVersion: cluster.x-k8s.io/v1beta2
kind: Cluster
metadata:
  name: burst
  namespace: caph-system
spec:
  clusterNetwork:
    pods:
      cidrBlocks:
        - 10.42.0.0/16
    services:
      cidrBlocks:
        - 10.43.0.0/16
  controlPlaneRef:
    apiGroup: controlplane.horizon.dev
    kind: ExternalControlPlane
    name: burst
  infrastructureRef:
    apiGroup: infrastructure.cluster.x-k8s.io
    kind: HetznerCluster
    name: burst
