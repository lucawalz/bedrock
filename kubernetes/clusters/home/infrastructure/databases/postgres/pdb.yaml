apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: postgresql
  namespace: postgres
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: primary
      app.kubernetes.io/instance: postgres-postgresql
      app.kubernetes.io/name: postgresql
