apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: litellm
  namespace: flux-system
spec:
  interval: 10m
  path: ./kubernetes/apps/litellm/app
  prune: true
  wait: true
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: cluster-edge-onprem
    - name: cluster-cnpg-db
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-settings
