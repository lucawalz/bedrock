apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: velero-ui
rules:
  - nonResourceURLs:
      - /readyz
      - /healthz
      - /livez
      - /version
    verbs:
      - get
  - apiGroups:
      - ""
    resources:
      - namespaces
      - pods
      - pods/log
      - pods/status
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - velero.io
    resources:
      - "*"
    verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
      - delete
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: velero-ui
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: velero-ui
subjects:
  - kind: ServiceAccount
    name: velero-ui
    namespace: velero
